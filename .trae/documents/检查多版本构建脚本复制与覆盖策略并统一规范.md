## 现状核查
- 代码库仅存在顶层 `Dockerfile`：`/Users/zest/myworks/emuhub/Dockerfile`，它复制：
  - 版本工具：`COPY android-docker/android34/tools /opt/tools`（Dockerfile:43）
  - 公共工具：`COPY android-docker/common/tools /opt/tools`（Dockerfile:44，已新增）
- 各版本目录（android28–android34）目前未发现各自的 Dockerfile（目录列举中无 `Dockerfile`），因此本仓库的构建流程只走顶层 `Dockerfile`，不存在“多版本各自 COPY 覆盖”的并行构建问题。
- 我们已将三脚本迁移到公共目录，并删除 34 目录同名副本，避免冲突：
  - 公共目录：`android-docker/common/tools/{start-device-session.sh, run-emulator-with-profile.sh, start-emuhub.sh}`
  - 34 目录中已无这三脚本副本；顶层构建复制 34/tools 与 common/tools 到 `/opt/tools`，且公共目录复制在后，若存在重名，会被公共版本覆盖（当前无重名）。
- 运行时引用均指向 `/opt/tools`：
  - 会话接口：`web/pages/api/devices/[id]/start.js:78` 调用 `/opt/tools/start-device-session.sh`
  - 会话脚本内部：调用 `/opt/tools/run-emulator-with-profile.sh`
  - 容器启动：`Dockerfile:71` `CMD ["/opt/tools/start-emuhub.sh"]`

## 风险与规范建议
- 若未来确实存在每版本单独的 Dockerfile，各自复制版本 `tools`：
  - 建议在每个版本 Dockerfile 中增加 `COPY android-docker/common/tools /opt/tools`，并确保顺序在版本 `tools` 之后，以便公共脚本覆盖同名版本脚本。
  - 建议将版本目录保留仅版本特有的 `android-env.sh`（或其他确需不同的脚本），避免重复的通用脚本；所有运行脚本统一使用公共目录版本。

## 拟实施（本仓库）
- 保持顶层 Dockerfile 复制顺序：先复制 `android34/tools`，再复制 `common/tools`，确保公共脚本优先生效。
- 不对其他版本目录做修改（因未检测到对应 Dockerfile），仅在新建或引入时遵循上述规范。

## 验证
- 顶层构建后 `/opt/tools` 包含三公共脚本且可执行；接口和容器入口正常。
- 无重名覆盖冲突；如后续新增版本工具与公共脚本重名，公共复制在后保证公共版本生效。