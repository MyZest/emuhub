## 目标
- 仅迁移你指出的三个脚本到公共目录：
  - start-device-session.sh
  - run-emulator-with-profile.sh
  - start-emuhub.sh
- 更新 Dockerfile，让构建阶段复制公共目录脚本到 /opt/tools（并保留其它版本脚本的复制），同时删除版本目录中的这三份副本，规范位置。

## 具体变更
1. 新建目录 `android-docker/common/tools/`，把上述三脚本复制进去，内容与当前最新版保持一致。
2. 更新 `Dockerfile`：
  - 保留现有 `COPY android-docker/android34/tools /opt/tools`（用于其它版本脚本）
  - 增加 `COPY android-docker/common/tools /opt/tools`（公共脚本覆盖同名）
3. 删除 `android-docker/android34/tools` 下这三份脚本副本，避免混淆。

## 引用与兼容
- 接口与启动命令均指向 `/opt/tools/...`，无需改动代码：
  - 接口：`/opt/tools/start-device-session.sh`
  - 会话脚本内部：`/opt/tools/run-emulator-with-profile.sh`
  - 容器命令：`/opt/tools/start-emuhub.sh`
- 其它脚本（如 `android-env.sh`、`android-sdk-update.sh`）继续从版本目录复制，保证构建完整。

## 验证
- 构建后 `/opt/tools` 下存在三脚本来自公共目录；接口与容器入口均可正常调用。
- 版本目录不再包含这三脚本的冗余副本。